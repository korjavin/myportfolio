# Keep the build context to what `go build` and `go:embed` actually read.
.git
.github
.beads
.dolt
.claude
.codex
.agents
docs
*.md
compose.yaml
Dockerfile
.dockerignore
.gitignore

# Local state that must never be baked into an image.
*.db
*.db-wal
*.db-shm
.beads-credential-key

# Build output
/myportfolio
*.test
