{"id":"int-b503e4f4adc328ea83c2fa09328a6693","kind":"field_change","created_at":"2026-07-28T17:24:37.894267207Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-59l","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Completed research and written comprehensive documentation in ./docs/"}}
{"id":"int-ea6c26a1782589ba2a04e64062e3d111","kind":"field_change","created_at":"2026-07-28T18:26:26.470376509Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.1","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged e61c003 (branch worktree-agent-a6718f49c5aa7fdcd, commit 29ad489). Go server skeleton: single origin (no subdomain/Host routing — verified by smoke test, a foreign Host header still gets 200), SQLite store with numbered migrations, static PWA serving via web/embed.go, /healthz + /readyz. go build/vet/test clean, CGO_ENABLED=0 static binary. Deferred and re-filed: rate_limit.go NOT ported (its constants are ceremonyRateLimit* and A1 has no ceremony endpoints — a limiter wrapping nothing is dead code plus a background ticker); A3 ports it near-verbatim, and the X-Forwarded-For clientIP handling there is load-bearing. accounts table deliberately minimal (id, created_at_unix) — A3/A5 hang their tables off it."}}
{"id":"int-1faefa3d8f947c8bf2cd59cf76d55eca","kind":"field_change","created_at":"2026-07-28T18:26:27.129015154Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.2","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged e61c003 (commit 29ad489). crypto.js ported to mp/v1 labels with salt_kek = SHA-256('myportfolio/v1/prf-kek'). Dropped per spec: oplog record crypto, push payload, MCP frame, X25519 inbox. Added encryptState/decryptState. 51/51 node --test, vectors pinned in vectors.json — and the executor mutation-checked them (flipping mp/v1/data and the state-version framing each fail the suite), so the pins genuinely bite rather than just being green. Tests are .mjs so they load as ESM regardless of package.json scope; verified 51/51 in all three scoping worlds. Resolved a spec gap: state AAD version framing is fixed 8-byte big-endian and means the version the blob WILL be stored as — now pinned in ARCHITECTURE.md §6 (335ef7f) as a wire contract A5/A6 must honour."}}
{"id":"int-a7409190970813e75c1d5ef236ebab9a","kind":"field_change","created_at":"2026-07-28T18:26:27.592357802Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.1","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged 3ddb2fd (branch worktree-agent-a0cd37dd82a8055fe, commits 196b12b/dee1881/f296415). The §3 records port over Dexie, §4 schema, §5 fixed-point money. Reviewed in depth: parseFixed parses digit-wise and never touches parseFloat; products go through BigInt and narrow only via a safe-integer check so precision loss throws instead of silently truncating; rounding is half-away-from-zero and consistent across the parse and divide paths including negatives; proportion(c,h,h)===c exactly, so closing a position leaves no basis dust. The port owns recordId/recordType/clientTs/deleted structurally via spread order, del writes a tombstone, and records.js holds no Dexie import so the rules test under plain Node. Codex found three real bugs, all confirmed empirically before fixing: non-positive shares built a short with positive basis; unpadded MM-DD keys won the latest-close race as strings; toFixed rounds the binary value and lost a cent on 1.005. Accepted web/package.json ({type:module}) as a necessary deviation — Node 18.19 cannot import ESM from .js without it; flagged by the executor rather than buried."}}
{"id":"int-95d3cc3f7d2648b9fff4ae18d37c8c82","kind":"field_change","created_at":"2026-07-28T18:26:28.008925815Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.2","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged 3ddb2fd. portfolio.js: folds the transaction log into positions, moving-average cost basis, realized/unrealized P/L, account balances, chunked-price valuation, asOf. Every §4 transaction type handled; oversell surfaced as an issue rather than clamped; mixed currencies raise currency_not_converted once per currency rather than silently summing unlike units. Fee/tax divergence (fees capitalise into basis, taxes never do) confirmed as intended and promoted into ARCHITECTURE.md §4 in 335ef7f, since it changes every realized-gain number. FIFO/LIFO deferred with a ponytail: comment naming the upgrade path. 51/51 node --test at merge; 102/102 across both tracks on the merged tree."}}
{"id":"int-415c4ef369f7e727d8613d88bf6d5b92","kind":"field_change","created_at":"2026-07-28T18:37:44.21080174Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.9","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged (branch worktree-agent-a7d2bc010400b1d86, commits 679d12e + 1ecf31f). Ported the --wg-* token system, both self-hosted font families, 10 domain-neutral primitives, the shared modal/toolbar/field utilities, and 6 guard tests. 230/230 on the merged tree; clean merge with no conflicts.\n\nVerified independently rather than trusting the report: injected a hardcoded hex and it failed 'no hardcoded hex colors outside :root'; deleted the gain glyph rule and it failed 'every delta modifier emits a sign glyph — gain/loss is never encoded by color alone'. The guards bite.\n\nBest call in this bead: the gain/loss accessibility requirement was implemented STRUCTURALLY, not as a convention. .wg-delta--gain/loss/flat::before emits the glyph from CSS and a test fails if it is removed, so a screens author cannot forget it and a future cleanup cannot silently delete it. That is stronger than what was asked for.\n\nClose judgment calls, all sound: wg-ring kept (generic gauge) but wg-ring-stack dropped (its accent tokens were medication-domain); wg-icons trimmed of pill/apple/dumbbell/etc and given layers+list for Holdings/Transactions; --tg-theme-* dropped but the legacy colour NAMES kept as aliases onto the wg palette so ported utility blocks carried over byte-identical.\n\nTwo guards deliberately widened beyond the sibling and now recorded in ARCHITECTURE.md §9: inline-styles covers every file with an EMPTY allowlist (no legacy to grandfather), and no-external-fonts covers every .html and .css plus asserts the named woff2 files exist. Both will bite future frontend executors, by design.\n\nDeferred with reasons: WGStaleBadge.mountFromKey (was bound to the sibling's ApiCache; render/formatLabel came over pure). domain-purity and sw-precache guards have nothing to bind to until the shell exists — moved to g7e.6. action-row.js kept verbatim including its emoji, flagged for later migration to the icon registry."}}
{"id":"int-0d0cdd9d77411d3d6f602c488da02479","kind":"field_change","created_at":"2026-07-28T18:57:43.786726032Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.3","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #1 (branch b3-perf-engine). TTWROR + IRR in web/domain/perf.js; consumes portfolio.js snapshot({asOf}), never re-derives positions. 247/247 at merge.\n\nVerified the headline claim independently rather than trusting it: ran my own bisection on Microsoft's XIRR example and the true root is 0.373362533519. The engine lands 1.9e-11 away; Excel's published 0.373362535 is 1.5e-09 away, and NPV at the published figure is -8.6e-6 versus ~zero at the engine's. The executor chased a 4e-9 discrepancy instead of widening the tolerance to make its test pass — the right instinct in a returns engine. Also checked the multi-root fixture algebraically: -1000x^2+2500x-1540 does factor at exactly 1.1 and 1.4.\n\nDeviated from the bead deliberately and correctly: bracket-and-bisect with no Newton step. Newton's failure modes here all return confident wrong numbers and the repair for each is the bracket the fallback must build anyway — less code and no silent-garbage path. Non-convergence returns an explicit result carrying no 'value', so a caller cannot accidentally render a number.\n\nTTWROR breaks at every flow day AND the day before it; that second boundary is what makes a mid-period deposit not move the result, and it is directly tested against the distorted 27.27% figure.\n\nCodex: one finding dismissed after actually running it (excluding transfers as 'internal' prints a phantom -40%, because portfolio.js really does report the total dropping while a transfer is in flight) — both directions now regression tests. One finding fixed and real: same-day matched transfer legs applied gross diluted a 10% day to 9.09%; flows now net per day."}}
{"id":"int-5aa95336761265785a370e693fd70200","kind":"field_change","created_at":"2026-07-28T19:05:54.075734133Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.4","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #2. web/domain/ppimport.js parses PP's .xml client file and CSV transaction export into §4 records, plus batches() for chunked writes. 256/256 at merge, CI green on go + node 18 + node 20.\n\nFixture provenance verified myself, since it is the load-bearing fact about this bead: the XML fixtures are byte-for-byte copies of PP's own test resources (portfolio-performance/portfolio @ 729a58e, file versions 63-69), attributed in THIRD-PARTY.md under EPL-1.0 with the upstream commit pinned. I scanned them for personal data before merging a public repo — synthetic accounts ('Konto', 'Depot 1'..'Depot 4'), well-known listed securities, no emails or IBANs. Not embedded in the binary (web/embed.go covers web/static only).\n\nCSV fixtures are SYNTHETIC and that is the honest limitation of this bead — PP's CSV output is locale-dependent and needs a running app. Every behaviour is traced to a line of CSVExporter.java, but no byte of real PP CSV has been through this code. Filed as follow-up.\n\nTwo format facts the executor found that would have silently broken the importer: the .xml is an XStream graph whose top-level accounts/portfolios lists are often nothing but relative-XPath reference='../..' pointers into objects defined deep inside a cross-entry (without resolution the importer sees an empty portfolio); and a trade is serialised twice, once per side, both pointing at one buysell cross-entry, so that node is the dedupe key. sourceRows == imported + merged + skipped is asserted on both real fixtures.\n\nCodex found three, all real, all fixed, none dismissed. The third was the executor's own and was exactly the failure this bead exists to prevent: PP's Delivery (Inbound/Outbound) had been folded in with cash transfers, producing transfer records carrying securityId — the shape §4 forbids, which portfolio.js silently drops while the report counted the row as imported. A silently dropped trade reported as success is the worst outcome available to an importer.\n\nFour decisions escalated rather than guessed, all ruled and now pinned in ARCHITECTURE.md: recordId is opaque/stable with derived ids for imports (time+random ids make idempotency impossible); §5's lossless claim corrected — it does not extend to FX, which PP stores as BigDecimal; a negative amount is legal only on the cash-only types and is a data error elsewhere; assetClass is optional because PP has none and guessing one silently mis-buckets an allocation chart."}}
{"id":"int-deb0a0595efdac17363ac84c9f0b90fc","kind":"field_change","created_at":"2026-07-28T19:15:25.360777064Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.3","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #3 (branch a3-a5-vault-server). Server: 002_vault.sql, store/vault.go, and internal/server/{rate_limit,session,webauthn,state,recovery}.go. Client: signup.js, unlock.js, ldk.js, vault-boot.js, vault.html. crypto.js untouched, as specified. All three CI jobs green.\n\nCold unlock is exactly one discoverable assertion as ARCHITECTURE.md §8.2 requires: login/begin returns no allowCredentials and login/finish answers {account_id, envelope} together. PRF is probed by an immediate get() on the new credential and aborts BEFORE finish, so a PRF-less authenticator leaves no account behind at all — better than the spec, which only required deleting the credential.\n\nThe finding that justified this bead's caution: the ported rate limiter was a COMPLETE NO-OP. Codex flagged it, the executor wrote a probe rather than reasoning about it, and one caller rotating X-Forwarded-For got 90 ceremonies through a limit of 30 — its own earlier smoke test had demonstrated the bypass and it had labelled it a feature. The sibling survives the same code only because it always runs behind Traefik; this binary is meant to be self-hosted and directly reachable, which is exactly the context a near-verbatim port drops. Now forwarded headers are trusted only from a configured trusted peer, defaulting to loopback. Deliberately NOT 'any private address' — with a published container port every request arrives from the Docker bridge gateway, which would re-open the bypass for every containerised deployment.\n\nI verified that fix by mutation rather than trusting it: reintroducing 'trust any peer' reproduces the exact failure (90 through a limit of 30) and fails both named regression tests. The guard is real.\n\nTwo further codex findings, both real, both fixed: a failed recovery upload unwound to the welcome screen and would have minted a second same-device passkey (a named landmine, reached by a path the executor had not considered); and loopback IP literals were handed the https scheme.\n\nHonest gaps carried forward: no real authenticator available, so the browser-side PRF probe, the PRF-less abort branch, signalUnknownCredential() and the kit download/print are unverified — ceremonies ARE driven end-to-end in-process with virtualwebauthn (real signatures, real discoverable assertion) plus a smoke run of the binary. Known degradation: srcdoc inherits the parent CSP so style-src 'self' blocks the kit's inline style, leaving the PRINTED kit unstyled (content intact, download fine) — correctly judged not worth loosening CSP on the page that holds the DEK. Deferred: no QR on the kit, no GET /api/envelopes (nothing needs it until A7), no envelope-MAC audit on unlock (a device-list concern, belongs to A7)."}}
{"id":"int-5727b2424c6624bc13d8423f3a1a02db","kind":"field_change","created_at":"2026-07-28T19:15:25.600313499Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.4","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #3 (branch a3-a5-vault-server). Server: 002_vault.sql, store/vault.go, and internal/server/{rate_limit,session,webauthn,state,recovery}.go. Client: signup.js, unlock.js, ldk.js, vault-boot.js, vault.html. crypto.js untouched, as specified. All three CI jobs green.\n\nCold unlock is exactly one discoverable assertion as ARCHITECTURE.md §8.2 requires: login/begin returns no allowCredentials and login/finish answers {account_id, envelope} together. PRF is probed by an immediate get() on the new credential and aborts BEFORE finish, so a PRF-less authenticator leaves no account behind at all — better than the spec, which only required deleting the credential.\n\nThe finding that justified this bead's caution: the ported rate limiter was a COMPLETE NO-OP. Codex flagged it, the executor wrote a probe rather than reasoning about it, and one caller rotating X-Forwarded-For got 90 ceremonies through a limit of 30 — its own earlier smoke test had demonstrated the bypass and it had labelled it a feature. The sibling survives the same code only because it always runs behind Traefik; this binary is meant to be self-hosted and directly reachable, which is exactly the context a near-verbatim port drops. Now forwarded headers are trusted only from a configured trusted peer, defaulting to loopback. Deliberately NOT 'any private address' — with a published container port every request arrives from the Docker bridge gateway, which would re-open the bypass for every containerised deployment.\n\nI verified that fix by mutation rather than trusting it: reintroducing 'trust any peer' reproduces the exact failure (90 through a limit of 30) and fails both named regression tests. The guard is real.\n\nTwo further codex findings, both real, both fixed: a failed recovery upload unwound to the welcome screen and would have minted a second same-device passkey (a named landmine, reached by a path the executor had not considered); and loopback IP literals were handed the https scheme.\n\nHonest gaps carried forward: no real authenticator available, so the browser-side PRF probe, the PRF-less abort branch, signalUnknownCredential() and the kit download/print are unverified — ceremonies ARE driven end-to-end in-process with virtualwebauthn (real signatures, real discoverable assertion) plus a smoke run of the binary. Known degradation: srcdoc inherits the parent CSP so style-src 'self' blocks the kit's inline style, leaving the PRINTED kit unstyled (content intact, download fine) — correctly judged not worth loosening CSP on the page that holds the DEK. Deferred: no QR on the kit, no GET /api/envelopes (nothing needs it until A7), no envelope-MAC audit on unlock (a device-list concern, belongs to A7)."}}
{"id":"int-b9f9198d5b43dd4f2580afc0fc0d5ebb","kind":"field_change","created_at":"2026-07-28T19:15:25.85473762Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.5","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #3 (branch a3-a5-vault-server). Server: 002_vault.sql, store/vault.go, and internal/server/{rate_limit,session,webauthn,state,recovery}.go. Client: signup.js, unlock.js, ldk.js, vault-boot.js, vault.html. crypto.js untouched, as specified. All three CI jobs green.\n\nCold unlock is exactly one discoverable assertion as ARCHITECTURE.md §8.2 requires: login/begin returns no allowCredentials and login/finish answers {account_id, envelope} together. PRF is probed by an immediate get() on the new credential and aborts BEFORE finish, so a PRF-less authenticator leaves no account behind at all — better than the spec, which only required deleting the credential.\n\nThe finding that justified this bead's caution: the ported rate limiter was a COMPLETE NO-OP. Codex flagged it, the executor wrote a probe rather than reasoning about it, and one caller rotating X-Forwarded-For got 90 ceremonies through a limit of 30 — its own earlier smoke test had demonstrated the bypass and it had labelled it a feature. The sibling survives the same code only because it always runs behind Traefik; this binary is meant to be self-hosted and directly reachable, which is exactly the context a near-verbatim port drops. Now forwarded headers are trusted only from a configured trusted peer, defaulting to loopback. Deliberately NOT 'any private address' — with a published container port every request arrives from the Docker bridge gateway, which would re-open the bypass for every containerised deployment.\n\nI verified that fix by mutation rather than trusting it: reintroducing 'trust any peer' reproduces the exact failure (90 through a limit of 30) and fails both named regression tests. The guard is real.\n\nTwo further codex findings, both real, both fixed: a failed recovery upload unwound to the welcome screen and would have minted a second same-device passkey (a named landmine, reached by a path the executor had not considered); and loopback IP literals were handed the https scheme.\n\nHonest gaps carried forward: no real authenticator available, so the browser-side PRF probe, the PRF-less abort branch, signalUnknownCredential() and the kit download/print are unverified — ceremonies ARE driven end-to-end in-process with virtualwebauthn (real signatures, real discoverable assertion) plus a smoke run of the binary. Known degradation: srcdoc inherits the parent CSP so style-src 'self' blocks the kit's inline style, leaving the PRINTED kit unstyled (content intact, download fine) — correctly judged not worth loosening CSP on the page that holds the DEK. Deferred: no QR on the kit, no GET /api/envelopes (nothing needs it until A7), no envelope-MAC audit on unlock (a device-list concern, belongs to A7)."}}
{"id":"int-a56dcd1e9f2c79453ebd2d781e1d6b3b","kind":"field_change","created_at":"2026-07-28T19:15:26.55572091Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.6","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #4. index.html, manifest.json + icons, sw.js, features/{boot,screens}.js, the two guard tests moved here from B9, shell CSS, and a fix to fonts.css. CI green on all three jobs; 288 web tests on master after merge.\n\nVerified against a real embedded binary rather than asserted: five slots navigate with hash, active pill, document.title and screen body moving together; deep-link reload lands correctly; self-hosted fonts genuinely apply (document.fonts reports Space Grotesk loaded, not a fallback); OFFLINE COLD START PAINTED IN 55ms with the network off, zero failed requests, reading real counts from IndexedDB — TRANSACTIONS 1 not 2, so the tombstone was honoured. CACHE_VERSION bump stages v2 in a separate cache while v1 keeps serving, prompts, and 'Later' changes nothing.\n\nFound a real bug on master and fixed it: fonts.css referenced /static/fonts/... but embed.go roots the FS at web/static, so every glyph 404'd. Invisible because font-display: swap silently keeps the fallback — the app looked fine while shipping none of its own typography. I confirmed both the bug and the fix by curling the running binary.\n\nCodex P1 was valid and the executor measured it rather than arguing: dismissing the update prompt does not pin the code — close every window and the staged worker activates without consent. It fixed the CLAIM rather than the mechanism, with the right reasoning: a worker that refuses to serve past an acceptedVersion does not bind the adversary §8 names, because the origin serves sw.js too. That correction has been promoted into README and ARCHITECTURE.md §8.\n\nDeferred: no Lighthouse verdict (no Chrome UI available — installability inputs verified, not the badge), no real home-screen install or iOS Safari exercise, no in-app install affordance (belongs on Settings, g7e.7)."}}
{"id":"int-7fad22d469db081f74759aec03bca273","kind":"field_change","created_at":"2026-07-28T20:17:12.259382588Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.11","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"License texts vendored beside the components they cover: Dexie 3.2.7 (Apache-2.0), both OFL fonts, EPL-1.0 for the PP fixtures, and now Lucide (ISC) for the icon path data in wg-icons.js. Everything under web/static is compiled into the binary by go:embed, so a build IS a redistribution and both Apache-2.0 §4 and OFL require the text to travel with the work; the minified Dexie had its own header stripped, so that file is the only notice the artifact carries.\n\nLucide provenance was confirmed, not assumed — the chevron path data (m15 18-6-6 6-6, m9 18 6-6-6-6, m6 9 6 6 6-6) matches upstream verbatim. Header comment in wg-icons.js points at the text, since the paths live inside a source file rather than a standalone vendored asset.\n\narchitecture.vendor-licenses.test.js guards against drift in both directions (unregistered asset, and asset removed while left in the manifest); verified it bites on an unregistered file. THIRD-PARTY.md's 'Known compliance gap' section is gone because the gap is gone."}}
{"id":"int-d3ae1beed19169f4018d71c50ee5acfd","kind":"field_change","created_at":"2026-07-28T20:17:12.9712748Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.15","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Closed by the same commit. architecture.asset-paths.test.js resolves every href/src/url() in every shipped .html and .css against the embedded tree, and separately names the /static/ prefix so the next author gets the reason rather than rediscovering it. Verified it bites by reintroducing the exact historical bug in design.html — 2 failures, then reverted clean. The rule is now written down in ARCHITECTURE.md §9 ('Assets are served from the root, not /static/'), which is the actual root cause: two independent authors made the same mistake because embed.go's rooting was never documented.\n\nWorth noting why this needed a test rather than a review: neither historical failure was visible at runtime. font-display:swap silently kept the fallback font, so the app looked correct while shipping none of its own typography."}}
{"id":"int-f8276aaf984c73ff231ccdc6d1640ed1","kind":"field_change","created_at":"2026-07-28T20:49:50.931591021Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.6","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #5. state-sync.js (the §6 wire protocol, no storage) + vault-records.js (the §3 port). 353/353 on master after merge, Go green, CI green on all three jobs.\n\nVerified the heart of the bead myself rather than trusting it: property-tested mergeRecords over 5000 random pairs — commutative across 738 collisions with EQUAL clientTs, idempotent, and a newer delete never loses. (My first run reported non-commutativity; my generator was wrong, not the code — it emitted duplicate recordIds within one side, which the Dexie keyPath makes impossible.)\n\nThe best judgment in this bead is the deterministic tiebreak. Without one, each device keeps its own side of a clientTs tie, a merge that changes nothing locally never schedules a push, and two devices sit on permanently different data with NOTHING to detect it. Comparing canonical forms picks the same winner on both sides for free. That is the failure mode most LWW implementations ship.\n\nIt also declined codex's suggested fix for the shared-metadata finding, with better reasoning than the suggestion: keying metadata by account would be WORSE, because localdb.js is one un-scoped Dexie database per origin, so a second account starting with clean versions on top of the first account's records converts a loud stop into a silent cross-vault upload. Instead the metadata carries its owning account and refuses a mismatch before touching the wire — and claims the mirror on OPEN rather than on first successful sync, because a vault used entirely offline never reaches the network, so a guard that waits for a response is absent exactly when it is needed.\n\nEvery load-bearing guarantee has a named test: rollback refusal, bounded retry then a real error, never drops a write, 401 distinguished from offline, tombstones retained, clock-skew correction, and the monotonic guard ('does not lose the edits of a device whose clock is ten minutes slow').\n\nHonest gaps carried forward, all filed: nothing wires this up yet (A12), the mirror is not account-scoped (A13), and the metadata lives in its own IDB rather than §3's sync_meta because localdb.js was off-limits (recorded in the contract instead of pretending). Test files are split three ways to dodge a Node 18 runner wedge the executor could not narrow; documented at the top of the file so nobody merges them back. Not correctness — CI passes on both Node 18 and 20."}}
{"id":"int-5fca6aa3f07f60c5ebd9052dde942fcb","kind":"field_change","created_at":"2026-07-29T06:20:52.752412954Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.11","extra":{"field":"priority","new_value":"1","old_value":"3"}}
{"id":"int-be5b8c96621329f72992e7b445411f46","kind":"field_change","created_at":"2026-07-29T06:20:53.3797162Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.12","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Folded into myportfolio-g7e.11 rather than run separately. Both change the same fold in portfolio.js, and sequencing them as two beads means restructuring the same code twice — the date validation and the CASH_SIGN export are now acceptance criteria on the restructure. Nothing is dropped."}}
{"id":"int-0d4c1f35b87c5d69ef7c84f104ee6ac4","kind":"field_change","created_at":"2026-07-29T06:32:48.413898182Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.7","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #6 (branch g7e.7-screens). All five screens over the existing domain modules: dashboard, holdings, transactions CRUD, performance, settings with PP import/export. 19 files, ~3000 lines. 425/425 on master after merge, Go green, CI green on all three jobs.\n\nThe executor committed locally and then stopped WITHOUT pushing or opening a PR — the 'finished but unshipped' failure I had been watching for. I pushed it, opened the PR, and ran the codex pass it never got to. Worth noting for future waves: a local commit with no PR reads as 'no work done' from the branch list, and I nearly mistook it for lost work (the branch name was g7e.7-screens, not the worktree-derived name I was grepping for).\n\nVerified structurally rather than by reading claims:\n - The money boundary is one-way AND enforced. forms.js imports only parseFixed/formatFixed (exact inverses) and deliberately does not import fmt.js, whose grouped '1,234.56' output is display text — parsing display text back into money is the bug that silently corrupts a portfolio. features.forms.test.js fails if any feature module imports toFloat; I confirmed the guard bites by adding the import ('not ok 1 - no feature module imports toFloat').\n - Position identity is opaque, per the mid-flight multi-account decision. Holdings renders from the engine's list and never keys by securityId, with a comment naming g7e.11, so re-keying by (accountId, securityId) turns one row into two without touching the file. The two position.securityId uses are actions (add a price, prefill a buy), not identity.\n - No hardcoded cost-basis label, so settings.costBasisMethod lands without a copy change.\n\nCodex found two P2s, both confirmed in the code before fixing (8cb4a3f). The serious one was a CREDENTIAL LEAK: the API key field was populated once from the initially-active provider and never updated when the select changed, so switching provider and saving stored the old provider's key under the new one — the next fetch would send a Finnhub credential to Alpha Vantage. A password field hides that happening. The second: a successful import wrote its report to a slot importRecords() had already detached via refresh(), so the user saw nothing and would import again."}}
{"id":"int-a5e85520b4c74dec715bb05a1c6c6fc5","kind":"field_change","created_at":"2026-07-29T06:50:51.183862952Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.14","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #8. Dockerfile (CGO_ENABLED=0 -> scratch, 5.3 MB, USER 65532), compose.yaml, .github/workflows/release.yml (new; ci.yml untouched), docs/DEPLOY.md, README section. CI green.\n\nThe acceptance criterion was DRIVEN, not reasoned about — real Traefik v3.6, isolated network, client containers on pinned IPs, limit 30/min: client A got 30x200 + 5x429 and spent its budget; client B on a different IP got its own 8/8; client C rotating forged X-Forwarded-For/X-Real-IP bought nothing. Crucially it has a NEGATIVE CONTROL: with loopback-only trust, client E gets 8/8 429 from client D's traffic. A test that cannot fail proves nothing, and this one can.\n\nMYPORTFOLIO_TRUSTED_PROXIES is declared with compose's ${...:?}, so a stack missing it REFUSES TO DEPLOY rather than coming up quietly wrong. And compose publishes NO ports — load-bearing rather than tidy, because trusting the Traefik subnet is only honest while nothing outside that network can reach the app.\n\nTwo bugs found only by running it: COPY --from resets ownership to root regardless of the builder's chown, so first boot against an empty volume crash-looped on 'unable to open database file'; and Docker reused a freed container IP so the first 'client B' was literally client A — a false negative the executor caught and redid with pinned IPs rather than reporting as a pass.\n\nCodex found three, all valid, all fixed. Two were P1 and both are supply-chain: (1) a push trigger runs ALONGSIDE CI rather than after it, so red master would still ship — now workflow_run on CI success, building head_sha (the tested commit, since github.sha on workflow_run is the default branch head at dispatch time); (2) workflow_run's branches: filter matches the TRIGGERING run's head branch, and ci.yml also runs on pull_request — so a fork PR from a branch named 'master' produced a successful CI run whose head_branch was master and reached a job holding contents:write + packages:write. Now gated on event=='push' AND head_repository==this repo. I verified that triple in the merged file; it is correct.\n\nIt also caught two errors in my own §10, both confirmed: the app never reads X-Forwarded-Proto (zero hits in internal/ and cmd/), and it already sets HSTS itself at internal/server/server.go:107, so listing it as an edge requirement invited two sources for one value. §10 corrected.\n\nNot exercised, stated plainly in the docs and PR: a real Let's Encrypt cert, a full passkey ceremony (no browser), the GHCR push, the Portainer webhook."}}
{"id":"int-66020306d0bf4c42515910a9b16dd091","kind":"field_change","created_at":"2026-07-29T06:50:51.895342924Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.5","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #9. web/domain/quotes.js + tests, nothing else touched. 446/446 on master, CI green.\n\nProvider: Twelve Data, chosen with a real reason rather than a coin flip — of §7's three candidates it is the only one whose free tier still serves daily history from the SAME endpoint as the latest close (/time_series), so 'current' and 'historical' are one code path with a days parameter instead of two that drift. Finnhub moved /stock/candle behind its paid tier; Alpha Vantage's 25 req/day with no batching would be exhausted by one refresh. Twelve Data also takes comma-separated symbols, so N holdings cost one round trip. A third provider is one table entry plus a load function — no adapter framework, as instructed.\n\nFixture provenance, which is the verification fact that matters here: CoinGecko market_chart and the Twelve Data single-symbol response + its 401 body were CAPTURED LIVE (verbatim values and timestamps). The Twelve Data MULTI-symbol envelope is written from the docs, because the public demo key rejects comma-separated symbols — the executor flagged that as the one shape a real key should re-verify rather than letting it pass as captured. It also ran the module end to end against both live APIs and confirmed both send access-control-allow-origin: *.\n\nPrivacy constraint held: I verified QUOTE_HOSTS is exported and frozen, both hosts are external and hardcoded, and nothing reaches our own origin. That export is exactly what 18h.9 needs for the CSP connect-src allowlist instead of restating hostnames.\n\nMoney boundary verified: the only Number() in the file is on an HTTP error code; prices go through parseFixed at a single conversion point. MM-DD chunk keys are zero-padded structurally — sliced from a validated ISO date, so the padding is guaranteed by the source format rather than by a padStart someone can delete.\n\nCodex found three, all real, all fixed: one shared lastRequestAt let CoinGecko's request stall Twelve Data's first batch for a full 60s, freezing a mixed portfolio for a minute it did not owe; a per-symbol error in a multi-symbol response threw and discarded the whole batch, so one typo'd ticker cost seven good securities their prices; and a chunk-key construction that had disagreed between two sites, letting a PP-imported chunk be shadowed by a duplicate — now one helper.\n\nBeyond the brief and correct: 401/403/429 halts that provider for the rest of the run rather than retrying into the user's quota, reported per security as bad_api_key/rate_limited. There is no retry anywhere in the module."}}
{"id":"int-72a4df3f70a11fa09fa9c58f22bb6c57","kind":"field_change","created_at":"2026-07-29T07:26:44.729361526Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.13","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #10. THE LOOP IS CLOSED — the sync layer and the vault are no longer dead code. features/sync.js is the whole decision: it reads the warm LDK cache and either leaves localRecords in place or builds vaultRecords and hands it to the store. Nothing under web/domain/ changed, which is exactly what §3 exists for. 467/467 on master, Go green, CI green.\n\nThe load-bearing detail, and a genuine trap correctly avoided: store.js's records export is now a stable FAÇADE over a swappable impl. The domain factories capture the port object at module load, long before boot has decided anything — so reassigning the export would have left both engines pointing at localRecords forever. Dead sync that looks exactly like working software. I verified the façade shape in the merged file.\n\nI verified the migration claim rather than trusting it, because 'my data vanished when I signed up' is unrecoverable: the test writes four rows including a tombstone through the REAL createLocalRecords, boots the vault over the SAME mirror, decrypts the server blob and asserts all four are present with the tombstone intact. I mutated sync.js to build the vault over a fresh db and it failed that test plus three others — real coverage, not a restatement.\n\nCodex found two, both real, both fixed with a failing-first test. The second is the one that matters most: flush() with nothing dirty emits syncing true->false WITHOUT touching the network, so backgrounding the tab would stamp 'Synced just now' over a server unreachable for a week — the exact lie this affordance exists to prevent. Now a sync is only dated when there was pending work. The regression test covers both directions: a no-op flush still reads '3 days ago', a flush carrying a write dates itself. The first: reconnecting while the user sat in the foreground had no wake-up signal, so a write that failed offline waited for the next focus while the UI had promised otherwise.\n\nSurfacing is honest by construction: describeSync() is pure with every branch asserted, and its 'ambient' field marks the states a human must act on, which boot prepends to whatever screen is open. 401 -> 'your vault session expired' + Unlock, write stays pending. Offline -> stated, never nagged. Server unreachable WITH a network -> shown after a second failure. wrong-account -> the port is NOT swapped, so the mirror stays unsynced rather than uploading into the wrong vault. No green tick anywhere, and zero setInterval — pull on open, focus/visibilitychange (interval-gated so one alt-tab is one pull), and online.\n\nDriven end to end two ways against the real binary: a Node driver (real Go server, SQLite, CAS, AES-GCM+gzip — offline rows -> signup -> present in the blob -> second device pulls them -> a write on B reaches A on focus -> a real 401 keeps the write) and headless Chrome with real Dexie and a real service worker. NOT verified: a real WebAuthn ceremony, which is not headlessly drivable — both drivers substitute only its last step (write the LDK cache, set the session cookie, which the server then verifies for real)."}}
{"id":"int-ace33114d8a86115c153451c1f9158b9","kind":"field_change","created_at":"2026-07-29T10:32:58.649521818Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.16","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #11. settings.quoteProviders is edited as a MAP: quoteProviderRows(stored) and mergeQuoteProviders(stored, edits) are pure and exported, the merge spreads into the stored map rather than replacing it, and a provider absent from the rows is left exactly as stored — so minIntervalMs and anything else the card does not own survives.\n\nVerified by mutation rather than by reading: replacing the merge with a fresh object fails 4 tests, and adding a phantom provider to the offered list fails the drift pin. The pin is the good part — QUOTE_PROVIDERS is asserted equal to quotes.js's own exported QUOTE_HOSTS, so adding a provider in one place and forgetting the other fails here rather than silently pricing nothing.\n\nDropped finnhub and alphavantage, which quotes.js reports as unknown_provider and which therefore priced nothing with no error the user ever saw. An install that already stored one still gets a labelled row so it can be deleted, rather than an invisible credential.\n\nCodex found a genuine regression in the executor's own first pass and it took the better fix: it had scrubbed apiKey from keyless providers (because loadCoinGecko forwards a stored key as x_cg_demo_api_key, so an 8cb4a3f-misfiled Finnhub key would have gone to CoinGecko) — but a LEGITIMATE CoinGecko demo key raises the free-tier limit, and the two cases are indistinguishable from stored data. Guessing is wrong either way, so CoinGecko now has its own key field: untouched save keeps it, clearing removes it, nothing dies unseen.\n\nAlso bumped CACHE_VERSION v3->v4 in sw.js — settings.js is precached and the shell is cache-first, so installed PWAs would have kept the old screen. 484/484 on master, CI green on all three jobs."}}
{"id":"int-d55fdf46374772cb27dbac42ae7c79b3","kind":"field_change","created_at":"2026-07-29T10:32:59.504692267Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.11","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #12. The owner's two product decisions, plus the date bug and the CASH_SIGN duplication, as one sequenced pass over the fold. 497/497 on master, Go green, CI green.\n\nPositions are keyed by (accountId, securityId) with portfolioId as the shares leg and accountId still the cash leg, plus a new snapshot().securities aggregate. A trade naming only the cash account raises missing_portfolio and folds into an unattributed position rather than being guessed onto the only depot present — surfaced, not invented. The key is built in portfolio.js and nowhere else, and holdings.js/dashboard.js render two rows with NO UI change, which is exactly what their g7e.11 comments predicted.\n\nLots are tracked always, consumed oldest-first under both methods; settings.costBasisMethod only changes what is reported. I hand-checked the headline numbers rather than trusting them: two lots (10 @ EUR1000, 10 @ EUR2000), sell 5 for EUR1250 — FIFO takes the EUR100/share lot for a EUR750 gain, moving-average blends to EUR150/share for a EUR500 gain. Both correct, and cash moves -EUR1750 either way, which is right because cash does not depend on basis method. Full exit leaves zero basis dust under both.\n\ntransaction.date now validated with perf.js's exact rule (regex + Date.UTC round-trip, so 2024-02-30 is rejected), reusing the rule rather than inventing a second one. Mutation-checked: disabling the guard fails both 'folded nowhere at all' and 'does not contaminate the opening valuation'. That was the silent one — an undated record sorted before every real date and landed in every snapshot including the opening valuation, so the portfolio was wrong everywhere at once with nothing looking broken.\n\nCASH_SIGN exported and PP_DEBIT deleted, so there is one source of truth for which way a type moves cash.\n\nTwo P1s found and correctly filed rather than fixed, both in files this bead did not own: g7e.18 (perf.js overwrites per-security values instead of summing depots — measured -40% where the truth is +20%) and g7e.19 (the transaction form never writes portfolioId, so every manually entered trade lands unattributed).\n\nWorth carrying forward: two PP fixtures now report an oversell, and the tests assert it with the reason — those files only balance because they move shares between depots, which §4 cannot carry a basis across. Security-keyed positions hid that; depot-keyed positions expose it. It makes g7e.10 more valuable than it looked."}}
{"id":"int-3acfe6746735e5ceb2cd04f746439f75","kind":"field_change","created_at":"2026-07-29T10:44:22.933674164Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.12","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #13. localdb.js now opens myportfolio_<accountId>, and the sync metadata follows the mirror — shared metadata under per-account mirrors is the same bug one layer down. 512/512 at merge, CI green on all three jobs.\n\nMutation-checked both guards myself rather than trusting the report:\n - making the signup migration a no-op fails three tests, including 'pre-signup rows go to the account that signs up, and to no other'. That is the unrecoverable case ('my data vanished when I signed up') and it is genuinely covered.\n - claiming unconditionally fails 'upgrading: the shared mirror is A's portfolio, and B unlocking first must not take it'.\n\nThat second one is the best thinking in this bead and it came from codex, correctly accepted rather than argued with: on a device upgrading from the un-namespaced layout, the shared myportfolio database is usually a signed-in account's real portfolio — and this very change is what lets a SECOND account unlock, so 'B unlocks first' is a likely order, not an edge case. Claiming blindly would have moved A's trades into B's namespace and uploaded them.\n\nSignup is a MOVE, not a copy, so the next account does not inherit the same rows; idempotent, with the higher clientTs winning on collision (the §6 rule). Switching accounts RETAINS rather than wipes, documented at the decision site: a wipe destroys writes an offline device has not synced, making account switching a data-loss operation. The honest cost — A's plaintext mirror stays on disk — is stated rather than buried.\n\nThe §3 port contract did not move; state-sync.js, records.js and web/domain/** are untouched. The wrong-account refusal is kept as a backstop, unreachable in normal use, and I rewrote its user-facing copy: it told users to use a separate browser profile, which is now wrong advice for the only thing that path can still mean.\n\nHONEST GAP, and the reason to test this by hand before trusting it: the executor could NOT exercise the real Dexie/IndexedDB path — IndexedDB never initialises in that sandbox, and an unmodified master build stalls identically. Every test runs against an in-memory double. The logic is verified; the actual per-account database naming, migration and deletion against real IndexedDB are not. Filed as 18h.15."}}
{"id":"int-42e900be450cf16d07e76ce8d561a66b","kind":"field_change","created_at":"2026-07-29T10:44:23.675146335Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.18","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #14. perf.js's valuesOf() folds snapshot().securities instead of snapshot().positions, so a security held at two depots is one summed row rather than whichever position folded last. 514/514 on master, CI green.\n\nVerified the bite properly. My first attempt was vacuous — 'git stash push -- domain/perf.js' stashed nothing because the file was already committed on the branch, and I briefly read the resulting green run as the tests failing to bite. Restoring master's perf.js over the branch fails exactly the two new tests, so they are real.\n\nIt picked the aggregate route over summing locally, for the right reason: shorter diff, and it cannot drift from portfolio.js's own definition of 'the security's value'. It checked the aggregate carried everything valuesOf() needed before committing to that. It also caught a second instance I had not named — the row's meta lookup had the same overwrite bug, so with two depots the row's name and ticker came from whichever position folded last.\n\nNull stickiness holds because portfolio.js resolves a quote per SECURITY, so marketValue is null for every position holding it or for none — the aggregate is null exactly when the security is unpriced, never a partial sum. Both tests were written first and both were red on master, one with the bead's measured 120000 !== 240000 (the -40%) and one with 'actual: 0', the exact partial this exists to prevent."}}
{"id":"int-a77911c4bb5c0c39e216d3f40fc230f9","kind":"field_change","created_at":"2026-07-29T16:36:50.485358084Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.17","extra":{"field":"priority","new_value":"1","old_value":"2"}}
{"id":"int-0bb08494f19548bab848fd7fb5c2405c","kind":"field_change","created_at":"2026-07-29T16:36:52.109055183Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.8","extra":{"field":"priority","new_value":"2","old_value":"3"}}
{"id":"int-a957622475d91d352f0b6ba609ad9063","kind":"field_change","created_at":"2026-07-29T16:36:52.758206934Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.8","extra":{"field":"priority","new_value":"3","old_value":"2"}}
{"id":"int-469cff3677efcbfdf497cc2a0bf3ea53","kind":"field_change","created_at":"2026-07-29T16:56:14.899031435Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.17","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #15. compose.yaml now declares MYPORTFOLIO_TRUSTED_PROXIES as ${...:-} instead of ${...:?}; .env.example comments it out and drops the invented 172.20.0.0/16 placeholder; DEPLOY.md and README no longer call it required. A Portainer stack now needs exactly one variable, MYPORTFOLIO_HOST — it was two.\n\nDriven rather than read, all three cases: nothing set refuses (MYPORTFOLIO_HOST still required); only MYPORTFOLIO_HOST resolves with an empty trusted-proxies value; an explicit override still flows through to the resolved config.\n\nWhat deliberately did NOT change, and I verified by diff: internal/server/rate_limit.go, its defaultTrustedProxies (still loopback-only) and its tests are untouched. Widening the Go default would have weakened a real control — it was considered and rejected, and the executor was told so explicitly so it would not 'improve' its way into it. And compose still publishes no ports, with the comment intact: that is the guard actually carrying the weight, because with no published port the bridge-gateway forgery path this setting defends against cannot be reached at all."}}
{"id":"int-361cfde90c86edb84674b8820e62e8c2","kind":"field_change","created_at":"2026-07-29T17:03:01.643764463Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.19","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #17. The transaction form collects and preserves portfolioId, closing the missing half of the g7e.11 position-keying restructure — every manually entered trade previously raised missing_portfolio and folded into an unattributed position. 527/527 at merge.\n\nVerified by mutation, not accepted: forcing the record->form direction to drop portfolioId fails 5 tests including 'a full record round-trips field for field'. That is the same bug class as the currency one already fixed here, where a no-op Save silently corrupted an imported record.\n\nTwo process notes worth keeping. The executor committed four commits locally and stopped without pushing — the third time this session — so I pushed and opened the PR myself. And the branch was 13 commits behind master, which made a naive master..HEAD diff read as though it were deleting the per-account mirror, the perf fix and the trusted-proxies change; the merge-base diff was 8 files, exactly the granted ownership. Anyone reviewing a stale executor branch should use three-dot diff."}}
{"id":"int-2da754aca87b0e771490b8599ae26625","kind":"field_change","created_at":"2026-07-29T17:03:02.47402998Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-18h.16","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #18. POST /api/recover plus the enroll begin/finish pair: typed code -> parseRecoveryCode -> deriveVerifier -> constant-time compare against the stored hash -> envelope_rec -> deriveKEKRec unwraps the DEK -> new passkey -> forced rotation. 536/536 web, Go green.\n\nThis closes a real hole, not a nice-to-have: signup generated a 160-bit code, printed an Emergency Kit and made the user tick 'I saved it', and nothing could redeem it. Losing your only passkey lost the vault permanently while onboarding had already promised otherwise.\n\nRotation and enrollment commit in ONE transaction, so the worst outcome — a working new passkey with the old code still live — is unreachable; the test asserts a rejected finish enrolls no credential AND leaves the verifier unrotated. Reuses the existing limitByIP rather than a second limiter, so the trusted-proxy handling stays in one place. Four tests nobody asked for and all correct to have: a second grant cannot undo the first rotation, a grant for another account is rejected, the enroll challenge is single-use, and the grant expires on a separate key.\n\nOne honest note carried into a follow-up: I mutation-tested the len(Recovery.Verifier)==0 guard by deleting it and the suite stayed green — it is redundant behind the envelope validity check, which rejects the same request first. Shipped behaviour is correct; the test just does not isolate that line. A case with a valid envelope but an empty verifier would pin it."}}
{"id":"int-066538b46aa5b1af45365a09983b5a1e","kind":"field_change","created_at":"2026-07-29T17:04:51.457339168Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g4m","extra":{"field":"status","new_value":"closed","old_value":"open","reason":"Already satisfied: the parallel sw.js executor bumped CACHE_VERSION v4 -> v5, which merged into the A16 branch (7248ee5). A new cache generation deletes myportfolio-shell-v4 on activate, so signup.js and vault-boot.js are refetched fresh and the recovery page loads."}}
{"id":"int-c072cd343d2e87b9eeffb9d56d5025ea","kind":"field_change","created_at":"2026-07-29T17:13:37.430712107Z","actor":"Korzhavin Ivan","issue_id":"myportfolio-g7e.22","extra":{"field":"status","new_value":"closed","old_value":"in_progress","reason":"Merged via PR #19. web/domain/prices.js (series(securityId, {from,to}) over that security's own §4 chunks — no snapshot, no fold, O(years)) plus features/pricechart.js hung off the existing position modal, rendered with the already-ported wg-sparkline. 545/545 on master, Go green, CI green.\n\nVerified against the real binary rather than reasoned: /domain/prices.js, /js/features/pricechart.js and /js/components/wg-sparkline.js all serve 200 and the sparkline script tag is present in the served shell. Worth noting — wg-sparkline had been ported since the design-system bead and was NEVER ACTUALLY LOADED; this branch is what first gives it a script tag and a precache entry.\n\nThe best thing in this branch is how it handled the reuse constraint I set. portfolio.js's chunk validation is private and portfolio.js was off-limits, so it could not import it. Instead of silently duplicating, prices.js exports eachClose() shaped so latestCloses() becomes a 5-line consumer of it, and a test pins the two readers against each other. I mutation-checked that pin: flipping the duplicate rule from first-wins to last-wins fails exactly one test — 'the last point agrees with the quote portfolio.js values the position at'. That is the drift guard doing its job. Follow-up myportfolio-22k carries the actual dedup edit.\n\nCodex found two, both real. The duplicate-same-day case is genuinely reachable: ppimport mints price_<hash> and store.putPrice mints price_<securityId>_<year>, so importing a PP file and then typing a close by hand leaves two chunks for one security-year — the chart's latest close and the holdings row's valuation would have come from different records. The second finding is the subtle one and the executor said it would have dismissed it on memory: it HAD bumped CACHE_VERSION v4->v5, but the recovery branch landed the same bump first, so the merge kept one v5 and the net change against master was zero while three assets joined PRECACHE. Now v6, confirmed v6-vs-master-v5.\n\nNOT verified: a real browser. No chromium/firefox in the sandbox, so 360px layout and on-screen appearance are reasoned from the stylesheet. The card's emitted markup and path geometry WERE read under a DOM shim across five fixtures (5y/1876 closes downsampled to 120 nodes, single close centred, empty, malformed-only, two-close falling)."}}
